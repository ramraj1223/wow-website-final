Drop `best-practices-guide.pdf` here.

This is the PDF referenced by the price estimator's "free guide" section copy. It isn't linked anywhere in this repo's code yet — the email that's supposed to deliver it is sent by automation outside this repo (Zapier / Supabase Edge Function / etc.), so whoever configures that automation should point it at `downloads/best-practices-guide.pdf` once the real file is here.
